Base images stack
=================

Usually you will build `FROM` these images, the stack is just for demo and testing.

    docker-compose up -d
    
    docker-compose ps
    
    docker-compose logs
